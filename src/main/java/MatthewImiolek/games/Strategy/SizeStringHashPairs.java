package MatthewImiolek.games.Strategy;

import java.util.HashMap;

// Holds a hashmap of all of the potential map size names and their hashes CURRENTLY UNUSEDS
public class SizeStringHashPairs {
    HashMap sizeMap = new HashMap();

    SizeStringHashPairs(){
        sizeMap.put(1637070917, "small");
        sizeMap.put(1, "medium");
    }
}
